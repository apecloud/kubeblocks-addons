# Default values for tidb.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  registry: apecloud-registry.cn-zhangjiakou.cr.aliyuncs.com
  repository: apecloud
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v7.1.5"
  helper:
    ## if the value of image.helper.registry is not specified using `--set`, it will be set to the value of 'image.registry' by default
    registry: ""
    ## if the value of image.helper.repository is not specified using `--set`, it will be set to the value of 'image.repository' by default
    repository: ""
    image: "busybox:1.35"
    pullPolicy: IfNotPresent

clusterVersionOverride: ""

clusterDomain: ".cluster.local"
