apiVersion: apps.kubeblocks.io/v1alpha1
kind: ClusterDefinition
metadata:
  name: tidb
  labels:
    {{- include "tidb.labels" . | nindent 4 }}
spec:
  topologies:
    - name: cluster
      default: true
      components:
      - name: tidb-pd
        compDef: tidb-pd
      - name: tikv
        compDef: tikv
      - name: tidb
        compDef: tidb
      # TODO: whether orders are necessary
