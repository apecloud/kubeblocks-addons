<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

<configuration>
    <property>
        <name>dfs.nameservices</name>
        <value>{{ .CLUSTER_NAME }}</value>
    </property>

    <property>
        <name>dfs.datanode.data.dir</name>
        <value>/hadoop/dfs/data0</value>
    </property>

    <property>
        <name>dfs.datanode.address</name>
        <value>0.0.0.0:{{ .DATANODE_DATA_PORT }}</value>
    </property>

    <property>
        <name>dfs.datanode.http.address</name>
        <value>0.0.0.0:{{ .DATANODE_HTTP_PORT }}</value>
    </property>

    <property>
        <name>dfs.datanode.ipc.address</name>
        <value>0.0.0.0:{{ .DATANODE_IPC_PORT }}</value>
    </property>

    <property>
        <name>dfs.client.failover.proxy.provider.{{ .CLUSTER_NAME }}</name>
        <value>org.apache.hadoop.hdfs.server.namenode.ha.ConfiguredFailoverProxyProvider</value>
    </property>

    <property>
        <name>dfs.ha.namenodes.{{ .CLUSTER_NAME }}</name>
        {{- $nns := "" }}
        {{- range $pod_name := splitList "," .NAMENODE_POD_NAME_LIST }}
        {{- $val := printf "nn%s" (splitList "-" $pod_name | last) }}
        {{- if eq $nns "" }}{{ $nns = $val }}{{ else }}{{ $nns = print $nns "," $val }}{{ end }}
        {{- end }}
        <value>{{ $nns }}</value>
    </property>
    {{- range $pod_name := splitList "," .NAMENODE_POD_NAME_LIST }}
    {{- $i := splitList "-" $pod_name | last }}
    <property>
        <name>dfs.namenode.rpc-address.{{ $.CLUSTER_NAME }}.nn{{ $i }}</name>
        <value>{{ $.NAMENODE_CLUSTER_COMPONENT_NAME }}-{{ $i }}.{{ $.NAMENODE_CLUSTER_COMPONENT_NAME }}-headless.{{ $.CLUSTER_NAMESPACE }}.svc.{{ $.CLUSTER_DOMAIN }}:8020</value>
    </property>
    <property>
        <name>dfs.namenode.http-address.{{ $.CLUSTER_NAME }}.nn{{ $i }}</name>
        <value>{{ $.NAMENODE_CLUSTER_COMPONENT_NAME }}-{{ $i }}.{{ $.NAMENODE_CLUSTER_COMPONENT_NAME }}-headless.{{ $.CLUSTER_NAMESPACE }}.svc.{{ $.CLUSTER_DOMAIN }}:9870</value>
    </property>
  {{- end }}
</configuration>
