apiVersion: v1
kind: ConfigMap
metadata:
  name: datanode-standalone-config-template
  labels:
    {{- include "hadoop.labels" . | nindent 4 }}
data:
  hdfs-site.xml: |-
    {{- .Files.Get "config/hdfs-datanode-standalone.tpl" | nindent 4 }}
  log4j.properties: |-
    {{- .Files.Get "config/log4j.properties" | nindent 4 }}
