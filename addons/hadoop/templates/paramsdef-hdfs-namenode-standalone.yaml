apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: namenode-standalone-config-constraints
  labels:
    {{- include "hadoop.labels" . | nindent 4 }}
spec:
  componentDef: hdfs-namenode-standalone
  templateName: namenode-standalone-config-template
  fileName: hdfs-site.xml
  fileFormatConfig:
    format: xml
  parametersSchema:
    topLevelKey: NameNodeParameter
    cue: |-
      {{- .Files.Get "config/hdfs-namenode-config-constraint.cue" | nindent 6 }}
