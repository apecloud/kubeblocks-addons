apiVersion: dataprotection.kubeblocks.io/v1alpha1
kind: ActionSet
metadata:
  name: {{ include "etcd.backupActionSetName" .}}
  labels:
    {{- include "etcd.labels" . | nindent 4 }}
spec:
  backupType: Full
  backup:
    backupData:
      image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.repository }}:v3.5.6
      runOnTargetPodNode: true
      syncProgress:
        enabled: false
        intervalSeconds: 5
      command:
      - /bin/bash
      - -c
      - |
        {{- .Files.Get "dataprotection/backup.sh" | nindent 8 }}
  restore:
    postReady:
    - job:
        image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.repository }}:v3.5.6
        runOnTargetPodNode: true
        command:
        - /bin/bash
        - -c
        - |
          {{- .Files.Get "dataprotection/restore.sh" | nindent 12 }}
