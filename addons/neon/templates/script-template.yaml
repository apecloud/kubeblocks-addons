apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "neon.scriptTemplateName" . }}
  labels:
    {{- include "neon.labels" . | nindent 4 }}
data:
  {{- include "neon.extend.script" . | nindent 2 }}
