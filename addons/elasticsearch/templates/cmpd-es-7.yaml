apiVersion: apps.kubeblocks.io/v1alpha1
kind: ComponentDefinition
metadata:
  name: {{ include "elasticsearch7.cmpdName" . }}
  annotations:
    apps.kubeblocks.io/skip-immutable-check: "true"
  labels:
    {{- include "elasticsearch.labels" . | nindent 4 }}
spec:
  serviceVersion: 7.10.1
  configs:
    - name: es-cm
      templateRef: {{ include "elasticsearch7.configTplName" . }}
      namespace: {{ .Release.Namespace }}
      volumeName: es-cm
  vars:
    - name: ELASTIC_USER_PASSWORD
      valueFrom:
        credentialVarRef:
          compDef: 
          name: elastic
          optional: false
          password: Required
    - name: ELASTIC_PASSWORD
      value: $(ELASTIC_USER_PASSWORD)
    - name: KIBANA_SYSTEM_USER_PASSWORD
      valueFrom:
        credentialVarRef:
          compDef: {{ include "elasticsearch7.cmpdName" . }}
          name: kibana_system
          optional: false
          password: Required
    - name: ELASTICSEARCH_HOST
      valueFrom:
        serviceVarRef:
          compDef: {{ include "elasticsearch7.cmpdName" . }}
          name: http
          host: Required
    - name: ELASTICSEARCH_ROLES
      value: master,data
  {{ include "elasticsearch.common" . | nindent 2 }}