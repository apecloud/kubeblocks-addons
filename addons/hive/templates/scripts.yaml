apiVersion: v1
kind: ConfigMap
metadata:
  name: hive-metastore-scripts
  labels:
    {{- include "hive.labels" . | nindent 4 }}
data:
  start-hive.sh: |-
    {{- .Files.Get "scripts/start-hive.sh" | nindent 4 }}
