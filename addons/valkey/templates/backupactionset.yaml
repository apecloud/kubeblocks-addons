{{/*
ActionSet for Valkey RDB+ACL snapshot backup and restore.

Backup flow:
  1. backupData: run BGSAVE on the target pod (secondary preferred), wait for
     completion, archive dump.rdb plus users.acl when present, and push with
     zstd compression.

Restore flow:
  1. prepareData: pull the archive, extract it into DATA_DIR, and seed the
     multipart AOF manifest/base/incr files from dump.rdb before the pod starts.
  2. postReady: fail-closed Sentinel monitor re-registration after data restore.

KubeBlocks DataProtection passes standard DP_* variables into all job containers;
DATA_DIR is injected from the ActionSet env. Current BackupPolicyTemplate env
schema only supports versionMapping for this chart path, so cross-component
Sentinel FQDN/password injection is not available here.
*/}}
apiVersion: dataprotection.kubeblocks.io/v1alpha1
kind: ActionSet
metadata:
  name: valkey-physical-br
  labels:
    clusterdefinition.kubeblocks.io/name: valkey
    {{- include "valkey.labels" . | nindent 4 }}
spec:
  backupType: Full
  env:
    - name: DATA_DIR
      value: {{ .Values.dataMountPath }}
    - name: TLS_MOUNT_PATH
      value: {{ .Values.tlsMountPath }}
  backup:
    preBackup: []
    postBackup: []
    backupData:
      image: $(IMAGE)
      runOnTargetPodNode: true
      syncProgress:
        enabled: true
        intervalSeconds: 5
      command:
        - bash
        - -c
        - |
          {{- .Files.Get "dataprotection/backup.sh" | nindent 10 }}
  restore:
    prepareData:
      image: $(IMAGE)
      command:
        - bash
        - -c
        - |
          {{- .Files.Get "dataprotection/restore.sh" | nindent 10 }}
    postReady:
      - job:
          image: $(IMAGE)
          onError: Fail
          runOnTargetPodNode: false
          command:
            - bash
            - -c
            - |
              {{- .Files.Get "dataprotection/post-restore-sentinel.sh" | nindent 14 }}
