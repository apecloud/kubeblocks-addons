apiVersion: apps.kubeblocks.io/v1
kind: ShardingDefinition
metadata:
  name: clickhouse
  labels:
    {{- include "clickhouse.labels" . | nindent 4 }}
  annotations:
    {{- include "clickhouse.apiVersion" . | nindent 4 }}
spec:
  template:
    compDef: {{ include "clickhouse.allCmpdRegexpPattern" . }}
  shardsLimit:
    minShards: 1
    maxShards: 128
  provisionStrategy: Parallel
  updateStrategy: Parallel
  systemAccounts:
    - name: admin
      shared: true
