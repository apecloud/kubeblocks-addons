# Default values for hive.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

spark:
  image:
    registry: docker.io
    repository: apecloud/spark-history
    tag: "v3.4.3-1.0.0"
    pullPolicy: IfNotPresent
