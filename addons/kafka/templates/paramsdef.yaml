apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: {{ include "kafka.paramsDefNameCombined" . }}
  labels:
    {{- include "kafka.labels" . | nindent 4 }}
spec:
  fileName: server.properties
  parametersSchema:
    topLevelKey: Combined
    cue: |-
      {{- .Files.Get "configs/kafka-server-constraint.cue" | nindent 6 }}
---
apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: {{ include "kafka.paramsDefNameController" . }}
  labels:
    {{- include "kafka.labels" . | nindent 4 }}
spec:
  fileName: server.properties
  parametersSchema:
    topLevelKey: Controller
    cue: |-
      {{- .Files.Get "configs/kafka-server-constraint.cue" | nindent 6 }}
---
apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: {{ include "kafka.paramsDefNameBroker" . }}
  labels:
    {{- include "kafka.labels" . | nindent 4 }}
spec:
  fileName: server.properties
  parametersSchema:
    topLevelKey: Broker
    cue: |-
      {{- .Files.Get "configs/kafka-server-constraint.cue" | nindent 6 }}
---
apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: {{ include "kafka2.paramsDefName" . }}
  labels:
    {{- include "kafka.labels" . | nindent 4 }}
spec:
  fileName: server.properties
  parametersSchema:
    topLevelKey: KafkaParameter
    cue: |-
      {{- .Files.Get "configs/2.7/kafka-server-constraint.cue" | nindent 6 }}
