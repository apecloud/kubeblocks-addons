apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgresql.patroniReloadScriptsTemplate" . }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
  annotations:
    {{- include "postgresql.annotations" . | nindent 4 }}
data:
  reload_config.sh: |-
    {{- .Files.Get "scripts/reload_config.sh" | nindent 4 }}
