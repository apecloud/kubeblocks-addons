apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "minio.scriptsTplName" . }}
  labels:
    {{- include "minio.labels" . | nindent 4 }}
data:
  {{- with include "minio.extend.scripts" . }}
  {{- . | nindent 2 }}
  {{- end }}