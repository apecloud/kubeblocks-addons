apiVersion: v1
kind: ConfigMap
metadata:
  name: falkordb-reload-tools-script
  labels:
    {{- include "falkordb.labels" . | nindent 4 }}
data:
  reload-parameter.sh: |
    {{- .Files.Get "scripts/reload-parameter.sh" | nindent 4 }}