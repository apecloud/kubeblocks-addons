# reuse falkordb4-config-effect-scope.
{{- range .Values.falkordbVersions }}
{{- $version := .major }}
{{- $falkordbImage := printf "%s/%s:%s" ( $.Values.image.registry | default "docker.io" ) $.Values.image.repository .defaultImageTag }}
{{- $pd := $.Files.Get "config/falkordb-config-effect-scope.yaml" | fromYaml }}
---
apiVersion: parameters.kubeblocks.io/v1alpha1
kind: ParametersDefinition
metadata:
  name: {{ printf "falkordb%s-config-pd" .major }}
  labels:
    {{- include "falkordb.labels" $ | nindent 4 }}
spec:
  componentDef: {{ printf "%s-%s" .componentDef  $.Chart.Version }}
  templateName: {{ printf "falkordb%s-config-template-%s" .major $.Chart.Version }}
  fileName: redis.conf
  fileFormatConfig:
    format: redis
  # ConfigurationSchema that impose restrictions on engine parameter's rule
  parametersSchema:
    topLevelKey: FalkorDBParameter
    cue: |-
      {{- $.Files.Get (printf "config/falkordb%s-config-constraint.cue" .major) | nindent 6 }}

  ## require db instance restart
  {{- if hasKey $pd "staticParameters" }}
  staticParameters:
    {{- $params := get $pd "staticParameters" }}
    {{- range $params }}
    - {{ . }}
    {{- end }}
  {{- end}}
{{- end}}
