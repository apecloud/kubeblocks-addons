immutableParameters:
  - clientPort
  - dataDir
  - dataLogDir
  - standaloneEnabled
  - reconfigEnabled
  - dynamicConfigFile

# If the staticParameters do not match, it is considered that the parameter is of the dynamicParameters type
#staticParameters:
#  - none

dynamicParameters:
  - none