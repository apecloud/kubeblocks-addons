## Parameters that require restart
staticParameters:
  - cluster-enabled
  - databases
  - io-threads
  - io-threads-do-reads

## Parameters that can be changed at runtime via CONFIG SET
dynamicParameters:
  - maxclients
  - timeout
  - loglevel
  - maxmemory-policy
  - appendfsync
