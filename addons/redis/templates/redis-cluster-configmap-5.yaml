apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-cluster5-config-template
  labels:
    {{- include "redis.labels" . | nindent 4 }}
data:
  redis.conf: |-
    {{- .Files.Get "config/redis-cluster5-config.tpl" | nindent 4 }}