apiVersion: dataprotection.kubeblocks.io/v1alpha1
kind: ActionSet
metadata:
  name: mongodb-rs-pbm-physical
  labels:
    clusterdefinition.kubeblocks.io/name: mongodb
    {{- include "mongodb.labels" . | nindent 4 }}
spec:
  env:
    - name: DATA_DIR
      value: {{ .Values.dataMountPath }}/db
    - name: MOUNT_DIR
      value: {{ .Values.dataMountPath }}
    - name: PBM_BACKUP_DIR_NAME
      value: "pbm-backups"
    - name: PBM_BACKUP_TYPE
      value: physical
    - name: PBM_COMPRESSION
      value: s2 # for big data, use s2 compression to reduce memory usage during restoration
    - name: PBM_RESTORE_DOWNLOAD_WORKERS
      value: "4"
    - name: PBM_IMAGE_TAG
      value: {{ .Values.image.percona.backup.tag }}
    - name: PSM_IMAGE_TAG
      value: {{ .Values.image.percona.tag }}
  backupType: Full
  backup:
    preBackup: []
    postBackup: []
    backupData:
      image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.percona.backup.repository }}:$(PBM_IMAGE_TAG)
      runOnTargetPodNode: true
      syncProgress:
        enabled: true
        intervalSeconds: 5
      command:
        - bash
        - -c
        - |
          {{- .Files.Get "dataprotection/common-scripts.sh" | nindent 10 }}
          {{- .Files.Get "dataprotection/rs-pbm-full-backup.sh" | nindent 10 }}
    preDelete:
      image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.percona.backup.repository }}:$(PBM_IMAGE_TAG)
      command:
        - bash
        - -c
        - |
          {{- .Files.Get "dataprotection/pbm-backup-delete.sh" | nindent 10 }}
  restore:
    prepareData:
      image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.percona.backup.repository }}:$(PBM_IMAGE_TAG)
      command:
      - bash
      - -c
      - |
        {{- .Files.Get "dataprotection/rs-pbm-restore-prepare.sh" | nindent 8 }}
    postReady:
    - job:
        image: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.percona.backup.repository }}:$(PBM_IMAGE_TAG)
        runOnTargetPodNode: true
        command:
          - bash
          - -c
          - |
            {{- .Files.Get "dataprotection/common-scripts.sh" | nindent 12 }}
            {{- .Files.Get "dataprotection/rs-pbm-full-restore.sh" | nindent 12 }}