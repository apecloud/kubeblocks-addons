apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "influxdb.configurationTemplate" . }}
  labels:
    {{- include "influxdb.labels" . | nindent 4 }}
data:
  config.toml: |-
    {{- .Files.Get "config/influxdb-config.tpl" | nindent 4 }}