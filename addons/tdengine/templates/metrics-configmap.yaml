apiVersion: v1
kind: ConfigMap
metadata:
  name: tdengine-metrics-configuration-template
  labels:
      {{- include "tdengine.labels" . | nindent 4 }}
data:
  taoskeeper.toml: |-
    {{- .Files.Get "configs/tdengine-taoskeeper-config.tpl" | nindent 4 }}
