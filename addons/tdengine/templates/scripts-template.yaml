apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tdengine.scriptsTemplate" . }}
  labels:
    {{- include "tdengine.labels" . | nindent 4 }}
data:
  taos-adapter-check.sh: |
    {{- .Files.Get "scripts/taos-adapter-check.sh.tpl" | nindent 4 }}
  entrypoint.sh: |
    {{- .Files.Get "scripts/entrypoint.sh" | nindent 4 }}
  start-keeper.sh: |
    {{- .Files.Get "scripts/start-keeper.sh" | nindent 4 }}
  reload-parameter.sh: |
    {{- .Files.Get "scripts/reload-parameter.sh" | nindent 4 }}
  create-mnode.sh: |
    {{- .Files.Get "scripts/create-mnode.sh" | nindent 4 }}
  role-probe.sh: |
    {{- .Files.Get "scripts/role-probe.sh" | nindent 4 }}
  member-leave.sh: |
    {{- .Files.Get "scripts/member-leave.sh" | nindent 4 }}