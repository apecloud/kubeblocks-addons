apiVersion: v1
kind: ConfigMap
metadata:
  name: orchestrator-config-template
  labels:
    {{- include "mysql.labels" . | nindent 4 }}
data:
  my.cnf: |-
    {{- .Files.Get "config/mysql5.7-config.tpl" | nindent 4 }}
