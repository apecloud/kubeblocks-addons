apiVersion: v1
data:
  orc-topology.cnf: |
    [client]
    user = {{- .Values.orchestrator.topology.username }}
    password = {{- .Values.orchestrator.topology.password }}
  orchestrator.conf.json: |-
    {{- .Files.Get "config/orchestrator.json" | nindent 4 }}
kind: ConfigMap
metadata:
  name: mysql-orc-config
