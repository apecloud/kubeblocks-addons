apiVersion: apps.kubeblocks.io/v1alpha1
kind: ClusterDefinition
metadata:
  name: {{ include "gbase.clusterDefName" . }}
  labels:
    {{- include "gbase.labels" . | nindent 4 }}
spec:
  topologies:
  - name: HA_mode
    default: true
    components:
    - name: gbase-ha
      compDef: {{ include "gbase.componentDefName" . }}-ha
