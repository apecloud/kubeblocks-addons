apiVersion: apps.kubeblocks.io/v1
kind: ComponentVersion
metadata:
  name: apecloud-postgresql
  labels:
    {{- include "orioledb.labels" . | nindent 4 }}
  annotations:
    {{- include "orioledb.apiVersion" . | nindent 4 }}
spec:
  compatibilityRules:
    - compDefs:
        - {{ include "orioledb.cmpdRegexpPattern" . }}
      releases:
        - 14.11.0
  releases:
    - name: 14.11.0
      serviceVersion: 14.11.0
      images:
        orioledb: {{ .Values.image.registry | default "docker.io" }}/{{ .Values.image.repository }}:14.11-0.7.3
