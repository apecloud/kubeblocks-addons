apiVersion: apps.kubeblocks.io/v1alpha1
kind: ClusterDefinition
metadata:
  name: apecloud-postgresql
  labels:
    {{- include "apecloud-postgresql.labels" . | nindent 4 }}
spec:
  topologies:
  - name: apecloud-postgresql
    default: true
    components:
    - name: postgresql
      compDef: apecloud-postgresql