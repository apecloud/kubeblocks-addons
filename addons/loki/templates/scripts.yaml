apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-scripts
  labels:
    {{- include "loki.labels" . | nindent 4 }}
data:
  {{- with include "loki.extend.scripts" . }}
  {{- . | nindent 2 }}
  {{- end }}
