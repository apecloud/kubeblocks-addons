apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fe-shared-nothing.configTemplate" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "starrocks.labels" . | nindent 4 }}
data:
  suspend: "none"
  fe.conf: |-
    {{- .Files.Get "config/fe-config-shared-nothing.tpl" | nindent 4 }}
    {{- include "params.priority_networks" . | nindent 4}}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fe-shared-data.configTemplate" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "starrocks.labels" . | nindent 4 }}
data:
  suspend: "none"
  fe.conf: |-
    {{- .Files.Get "config/fe-config-shared-data.tpl" | nindent 4 }}
    {{- include "params.priority_networks" . | nindent 4}}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "be.configTemplate" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "starrocks.labels" . | nindent 4 }}
data:
  suspend: "none"
  be.conf: |-
    {{- .Files.Get "config/be-config.tpl" | nindent 4 }}
    {{- include "params.priority_networks" . | nindent 4}}
  nginx.conf: |-
    {{- .Files.Get "config/nginx.conf.tpl" | nindent 4 }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cn.configTemplate" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "starrocks.labels" . | nindent 4 }}
data:
  suspend: "none"
  cn.conf: |-
    {{- .Files.Get "config/cn-config.tpl" | nindent 4 }}
    {{- include "params.priority_networks" . | nindent 4}}
  nginx.conf: |-
    {{- .Files.Get "config/nginx.conf.tpl" | nindent 4 }}
