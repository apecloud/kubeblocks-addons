image:
  registry: docker.io
  repository: chrislusf/seaweedfs
  tag: "4.47"
  pullPolicy: IfNotPresent

# KubeBlocks requires a semantic serviceVersion; the upstream image tag is 4.47.
serviceVersion: "4.47.0"

# Each SeaweedFS volume file is capped at 1 GiB; -max=0 derives the file count
# from free disk capacity. This is distinct from the Kubernetes PVC capacity.
volumeSizeLimitMB: 1024

extra:
  keepResource: false
