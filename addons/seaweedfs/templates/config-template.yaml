apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "seaweedfs.filerConfigName" . }}
  labels:
    {{- include "seaweedfs.labels" . | nindent 4 }}
data:
  filer.toml: |
{{ .Files.Get "configs/filer.toml" | indent 4 }}
