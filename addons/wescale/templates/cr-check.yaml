{{/*
{{- $namespace := .Release.Namespace }}
{{- if not (lookup "apps.kubeblocks.io/v1alpha1" "ComponentDefinition" $namespace .Values.etcd.etcdCmpdName) }}
  {{- fail (printf "The ComponentDefinition %s is required but does not exist in namespace %s." .Values.etcd.etcdCmpdName $namespace) }}
{{- end }}
*/}}