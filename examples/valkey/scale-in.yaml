apiVersion: operations.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: valkey-scale-in
  namespace: demo
spec:
  clusterName: valkey-replication
  type: HorizontalScaling
  horizontalScaling:
    - componentName: valkey
      scaleIn:
        replicaChanges: 1
