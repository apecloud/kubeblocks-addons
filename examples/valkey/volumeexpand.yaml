apiVersion: operations.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: valkey-volumeexpand
  namespace: demo
spec:
  clusterName: valkey-replication
  type: VolumeExpansion
  volumeExpansion:
    - componentName: valkey
      volumeClaimTemplates:
        - name: data
          storage: 30Gi
