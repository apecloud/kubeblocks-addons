apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-minio-minio-configuration
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    apps.kubeblocks.io/component-name: minio
data:
  MINIO_REPLICAS_HISTORY: "[]"