apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kblib.clusterName" . }}-namenode-hosts
  labels:
    app.kubernetes.io/name: {{ include "kblib.clusterName" . }}
    app.kubernetes.io/component: namenode
data:
  hosts.exclude: ""