apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kblib.clusterName" . }}-certificates
  labels:
    labels: {{ include "kblib.clusterLabels" . | nindent 4 }}
data:
  # provide ca certificate private key to generate PKCS12 format certificate
  ca-key.pem: {{ print "" | b64enc }}